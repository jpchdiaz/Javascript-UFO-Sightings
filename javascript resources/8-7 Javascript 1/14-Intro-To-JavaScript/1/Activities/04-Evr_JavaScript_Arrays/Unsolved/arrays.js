// arrays here
var firstArray = ["a", "b", "c", "d"]

// aLetter will be d
var aLetter = firstArray[3]

//array length
var lenArray = length(firstArray)

//to add
firstArray.push('e')

var firstThree = firstArray.slice(0,2)

var fStr = "this is a first string";
var sStr = "this is also a string";
//split and join
var splitFstr = fStr.split(" ");
newStr = fStr.join(sStr);
